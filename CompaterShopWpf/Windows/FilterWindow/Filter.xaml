<Window x:Class="CompaterShopWpf.Windows.FilterWindow.Filter"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Фильтр" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Выберите категории:" Grid.Row="0" Margin="5"/>
        <ListBox x:Name="categoryList" 
                 Grid.Row="1" 
                 ItemsSource="{Binding Categories}" 
                 SelectionMode="Multiple" 
                 Margin="5"
                 SelectionChanged="CategoryList_SelectionChanged"> <!-- Заменяем здесь -->
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding CategoryName}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Text="Диапазон цен:" Grid.Row="2" Margin="5"/>
        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="5">
            <TextBlock Text="Мин:" Margin="0,0,5,0"/>
            <TextBox Width="50" Margin="0,0,10,0" Text="{Binding MinPrice, Mode=TwoWay}"/>
            <TextBlock Text="Макс:" Margin="0,0,5,0"/>
            <TextBox Width="50" Text="{Binding MaxPrice, Mode=TwoWay}"/>
        </StackPanel>

        <Button Content="Применить" Click="Apply_Click" Grid.Row="4" Margin="5"/>
    </Grid>
</Window>
