<Window x:Class="CompaterShopWpf.Windows.AdminMenuWindow.EditCategory.EditItem"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:CompaterShopWpf.Windows.AdminMenuWindow.EditCategory"
        mc:Ignorable="d"
        Title="Редактировать товар" Height="300" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Название:" Grid.Row="0"/>
        <TextBox Text="{Binding Item.Name}" Grid.Row="1" Margin="0,5,0,10"/>
        <TextBlock Text="Описание:" Grid.Row="2"/>
        <TextBox Text="{Binding Item.Description}" Grid.Row="3" Margin="0,5,0,10"/>
        <TextBlock Text="Цена:" Grid.Row="4"/>
        <TextBox Text="{Binding Item.Price}" Grid.Row="5" Margin="0,5,0,10"/>
        <TextBlock Text="{Binding Item.Category.CategoryName, StringFormat = 'Категория: {0}'}" Grid.Row="6"/>
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="8">
            <Button Content="Отмена" Click="Cancel_Click" Margin="5"/>
            <Button Content="Сохранить" Click="Apply_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
